# Library Test

pause = [](); 
{ 
	return sys("pause"); 
};

getExeName = []();
{
	t = sys("window_title");
	c = sys("build_config");
	p = sys("build_platform");
	return (t + "_" + c + "_" + p + ".exe");
};