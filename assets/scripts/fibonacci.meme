# Script to show a Fibonacci sequence
{
	fib = [](num);
	{
		print("Fibonacci: {num}");
	
		if(num >= 0);
		{
			a = 0;
			print("{a}");
		
			if(num >= 1);
			{
				b = 1;
				print("{b}");
	
				next = a + b;
				while(next <= num);
				{
					print("{next}");
					a = b;
					b = next;
					next = a + b;
				};
			};
		};
	};

	fib(100);
}