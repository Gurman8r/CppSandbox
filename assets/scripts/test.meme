TEST = nodeid();
{
	# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #

	BODY = nodeid();

	fun = [](); { printl("Here"); };

	demo = $();
	{
		a = 1;
		b = 2;
		c = 3;
	};

	# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #

	main = [](args);
	{
		argc = sizeof(args);
		for (i = 0; i < argc; i = i + 1);
		{
			printl(args[i]);
		};

		i = 123;
		if ("int" == typename(i));
		{
			printl("It's an integer!");
		};

		if (true && true && false);
		{
			printl(true);
		};
		else;
		{
			printl(false);
		};
		
		if ("int" == typename(int));
		{
			printl("first");
		};
		
		printl(typename(typename(0.0)));

		printl(demo.a);
		printl(demo.b);
		printl(demo.c);
	};

	# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #

	main(__ARGS__);
	
	# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #
};