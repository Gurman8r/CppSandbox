TEST = nodeid();
{
	# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #

	BODY = nodeid();

	fun = [](); { printl("Here"); };

	demo = $();
	{
		a = 1;
		b = 2;
		c = 3;
	};

	# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #

	main = [](args);
	{
		if (argc = sizeof(args));
		{
			for (i = 0; i < argc; i = i + 1);
			{
				printl(args[i]);
			};
		};

		int = 123;
		flt = 4.56;
		str = "abc";
		ptr = s;

		if (true && true && false);
		{
			printl(true);
		};
		else;
		{
			printl(false);
		};
		
		if ("int" == typename(int));
		{
			printl("first");
		};
		
		printl(typename(typename(flt)));

		f = fun;
		f();
		fun();

		printl(demo.a);
		printl(demo.b);
		printl(demo.c);
	};

	# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #

	main(__ARGS__);
	
	# * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * #
};